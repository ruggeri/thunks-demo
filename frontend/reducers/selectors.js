export const allCats = (state) => (
  Object.values(state.cats)

  // const cats = [];
  // for (const catId of Object.keys(state.cats)) {
  //   const cat = state.cats[catId];
  //   cats.push(cat);
  // }
  // return cats;
);
